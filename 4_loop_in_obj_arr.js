// Object mai loop kaise lagay

const object= {
    js : "javascript",
    css : "style",
    html: "structured",
}
for (const key in object) {
    console.log(`${key} for ${object[key]}`)
}
